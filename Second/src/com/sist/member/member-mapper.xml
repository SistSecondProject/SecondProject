<?xml version="1.0" encoding="UTF-8"?>
<!-- 쿼리 작성 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.member.member-mapper">

<!-- /*
 * <member>
 * USERID
 PASSWORD
 NAME
 EMAILADDRESS
 FAVORITECATEGORY
 ZIPCODEADDRESS
 DETAILADDRESS
 USERLEVEL
 */-->
<!-- id 중복체크 -->
  <select id="IdCheck" parameterType="String" resultType="int">
        SELECT count(*) FROM member WHERE userid=#{userid}
    </select>
    
<!-- 우편번호  검색 --> 
   <select id="postfind" resultMap="list" parameterType="String">
   SELECT zipcode,sido,gugun,dong,
   NVL(bunji,' ') FROM zipcode 
   WHERE dong LIKE '%'||?||'%' 
   </select>
   
<!-- 동으로 검색 -->  
   <select id="postfindCount" resultType="int" parameterType="String">
   SELECT COUNT(*) FROM zipcode 
   WHERE dong LIKE '%'||?||'%'
   </select> 
   
<!-- join -->    
<insert id="memberJoin" parameterType="MemberVO">
  insert into member
  values(#{userid},#{password},#{name},#{emailaddress},#{favoriteCategory},#{zipcodeAddress},#{detailAddress},#{userLevel})
</insert>
</mapper>
